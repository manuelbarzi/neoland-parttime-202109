
function pop (array, element) {
       var element = array[array.length -1]
       // para que coja el ultimo elemento es el array.length - 1
       array.length = array.length -1
    return element
}