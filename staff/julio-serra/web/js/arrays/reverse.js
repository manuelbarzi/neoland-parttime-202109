function reverse(reverted) {
    // TODO
    var reverted = [] // para almacenar los datos
    var count = 0 // que empiece desde 0

    //contar las letras, agruparlas de derecha a izquierda, ir guardandolas en 
    //una variable para que al final nos lo retorne

    for (var i = array.length - 1; i >= 0; i--) 
    // declarar la variable que sera igual a la longitud del array - 1, para que empiece a 
    //contar del final al principio

    // que la iteracion sea mayor o igual que 0

    // ir restando 1 a la iteracion, del 5 a 4, del 4 a 3, así sucesivamente...
    {
    reverted[count] = array[i] // almacenamos los datos del array en count, que desde
    // el principio lo ponemos a 0, que será igual a la iteracion

    count ++ // para que sume 1, si empieza desde el 5, pasa al siguiente (el 4)
}
    return reverted // que nos devuelva el resultado

}
