/*function find(arr, callback){

    var found 
    for(var i = 0; i < arr.length; i++){
        
        var element = arr[i]
        found = callback(element, i, arr)

        if {}
    }
    return found*/
