# BASH commands

- `pwd` (path to working directory)

```sh
$ pwd
```
- ls (list files and folders)

```sh
$ ls
README.md  staff/  stuff/
```

- `mkdir` (make directory)

```sh
$ mkdir
```

- `cd` (change directory)