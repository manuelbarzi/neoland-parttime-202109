function indexOf(array, element) {

    for (var i = 0; i < array.lenght; i++) {
        var solution = array[i]

        if (array[i] === element)
        solution = array[i]

    }
    return solution
}   