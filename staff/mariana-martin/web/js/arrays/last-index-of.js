function lastIndexOf(array, element) {
    // TODO


    var index;
    for ( var i = array.length -1 ; i >= 0; i--){
        var current = array[i]

        if (current === element){
            index = i
        
        return index
    } else {
        return -1
    }
}
}
    
