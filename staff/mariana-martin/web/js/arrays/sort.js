function sort(array){


    var temp;
    for (var j = 0; j < array.length; i++){


        for ( var i = 0; i < array.length; i++){
            var a = array[i]
            var compare = array[i +1] 
        

        if (compare < a){
            temp = array[i]
            array[i] = array[i +1]
            array[i +1] = temp
            
  
        } else {
            array[i] 
            array[i +1]
            
        }}
        return array
}


}